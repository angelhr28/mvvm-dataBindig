<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <data>
        <variable
            name="viewmodel"
            type="com.example.sadanime.modulo.principal.viewModel.PrincipalViewModel" />
    </data>

    <LinearLayout
        android:id="@+id/root_recent"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        tools:context=".modulo.principal.view.fragment.RecentAnimeFragment">
        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/rcv_last_anime"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_marginStart="8dp"
            android:layout_marginLeft="8dp"
            android:layout_marginEnd="8dp"
            android:layout_marginRight="8dp"
            tools:listitem="@layout/item_animes"
            android:overScrollMode="never"/>

    </LinearLayout>
</layout>